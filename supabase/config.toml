project_id = "lista-de-presentes"

[auth]
site_url = "https://fernandogreca.github.io/lista-de-presentes/"
additional_redirect_urls = ["http://localhost:3000/lista-de-presentes/"]
enable_signup = false
enable_anonymous_sign_ins = false

[functions.extract-product]
# JWT is validated with Auth /user and exact admin UUID in the handler.
verify_jwt = false
